# Completion sound

`done.wav` is an original two-note chime generated by `generate.py`. It is
embedded in the tmux-sentinela binary and covered by the repository's MIT
license.

Run `python3 assets/sounds/generate.py` from the repository root to regenerate
it. Python is not needed to build or run the plugin.
